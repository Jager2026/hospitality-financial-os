{
  "name": "@hospitality-os/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node scripts/assert-still-placeholder.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
