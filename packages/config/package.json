{
  "name": "@hospitality-os/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared build/lint/tsconfig config, referenced by every app and package.",
  "files": ["tsconfig.base.json"]
}
