import type { JSX } from "react";
import { t } from "../../../lib/i18n";
import { Stub } from "../../_stub";

/** Create Your Restaurant — `UX_MAP.md`, "Getting In". Not built yet; it exists so the login
 * fork has a real destination to reach and the routing can be proved. */
export default function CreateRestaurantPage(): JSX.Element {
  return <Stub title={t("createRestaurant.title")} />;
}
