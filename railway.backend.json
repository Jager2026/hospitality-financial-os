{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter backend run prisma:generate && pnpm --filter backend run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter backend run start",
    "preDeployCommand": ["pnpm --filter backend run prisma:migrate:deploy"],
    "restartPolicyType": "ON_FAILURE"
  }
}
