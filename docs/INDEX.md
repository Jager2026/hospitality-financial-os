---
title: INDEX
status: Active
classification: Internal
owner: Founder
technical_owner: AI Technical Co-Founder
---

# Documentation Index

Purpose: one line per document — title, version, and what it actually contains — so any engineer (or Claude, at the start of a session) can find the right doc without opening all of them. This file is a map, not a source of truth: if it ever disagrees with the document it points to, the document wins and this line is stale.

Regenerate by hand when a doc's version bumps or a new doc is added — it isn't build-generated.

| Document | Version | What it contains |
|---|---|---|
| [MASTERPLAN.md](MASTERPLAN.md) | 2.5.0 | Product vision, business model, and scope for the Hospitality Financial Operating System — the top of the document hierarchy everything else refines. Includes "Pilot-Ready Product," two narrow, explicitly scoped exceptions to the Accounting-Integrations Out-of-Scope line (ADR-029), plus a third item recorded as a *candidate* gated on pilot evidence: restaurant branding on the guest-facing terminal, constrained to a logo and one accent from a pre-verified palette rather than the AI-generated free-form design originally proposed — with the AI kept as a chooser ("this accent suits your room") rather than a generator. Adds Product Positioning & Restaurant Value Proposition — a strategic specification rather than marketing copy: the core positioning line, Model B as the single target with the two-payout-modes hypothesis closed rather than deferred (ADR-053), restaurant value ordered by defensibility in three tiers with the trust layer qualified as a lead in time rather than a moat, the exact permitted tax formulation, switching cost named as the primary GTM obstacle because a terminal is tied to its acquirer and third-party integration is not established, six pilot limitations that may not be softened (POS reconciliation does not work), an SLA recorded as UNDEFINED rather than softened, and three unanswered Model B questions that block design. Tips by name now also says: paid at once, no holding period, and a refund returns the bill while the tip stays with the waiter (ADR-062). Restaurant value gains a third tier-1 item: the restaurant's day is not the calendar day, the owner is shown what arrived after midnight before the shift closed, and accounting gets both lists (ADR-064/065). |
| [AI_WORKFLOW.md](AI_WORKFLOW.md) | 2.2.0 | How the Founder and the AI Technical Co-Founder work: task format, one axis of risk per PR, the standing requirements, the words meant literally, how the queue is kept, and the five-point block closure. Opens with the working cycle — Founder writes the task, Claude Code works and reports, the Founder forwards the report to the architect, and Claude Code merges on approval, rebasing and waiting for CI itself. **The Founder does not open GitHub**, so nobody checks repository state before a merge and whatever a merge depends on must be established in the report. Plan mode as the committed default was reversed by the Founder on 2026-09-01 as too expensive (ADR-058 Amendment); the `ask`/`deny` rules are untouched, and step 9 points at `.github/pull_request_template.md`. |
| [CLAUDE_RULES.md](CLAUDE_RULES.md) | 2.18.0 | Behavioral and values contract for Claude as AI Technical Co-Founder — engineering culture, review standards, communication rules. Byte-identical to root `CLAUDE.md` per ADR-011 — now enforced by `repo-invariants.spec.ts` rather than asserted, after the two copies drifted apart unnoticed from PR #70 (ADR-045). |
| [ARCHITECTURE_DECISIONS.md](ARCHITECTURE_DECISIONS.md) | 1.47.0 | The full ADR log — forty-eight architecture decisions, each with context, decision, and consequences; the canonical record of *why*, including in-place revisions to earlier decisions (e.g. ADR-009, ADR-014, ADR-023, ADR-026, ADR-031). Sprint 13: ADR-032 (five THREAT_MODEL.md point fixes — ledger account reclassification, CI dependency scanning, idempotency race, breached-password check, payment reconciliation), ADR-033 (terminal staff selection replaces automatic tip recipiency), ADR-034 (Postgres major aligned across dev/CI/production, Railway config moved into git), ADR-035 (staging environment shape decided, build deferred; production data may never be copied into it), ADR-036 (git-triggered deploys as the normal path, gated manual railway up as an emergency hatch), ADR-037 (vitest 2→3 plus pnpm overrides — the audit ignore list is now empty), ADR-038 (boot-time secret shape validation plus an alerting liveness probe, after three hand-transferred secrets arrived corrupted), and ADR-039 (a staff member's Wallet gets its own route rather than a new filter on `GET /wallets` — a permission that had no addressable resource behind it for six sprints, and the recorded reasoning for why widening an existing route silently is the worst shape of permission change). ADR-034 carries an addendum: production was still minor-pinned to 18.6, which Railway reports as a hard blocker for PITR — at the time, the only backup mechanism in place (volume snapshots have since been configured alongside it). ADR-031 now collects three findings about tools reporting state misleadingly, the third being a manual volume snapshot with `expiresAt: null` that disappeared anyway, mechanism unknown — recorded for its consequence: a manual snapshot is not reliable insurance before a risky operation. Sprint 14 opens with ADR-040 (the Portal ships in English only — Lithuanian is sequenced rather than dropped, because it is the *terminal* that needs it; every string goes through a dictionary from day one, a decision about order rather than scope), ADR-041 (Playwright runs only on PRs touching the frontend — not to save minutes, but because a slow check that runs on everything gets worked around, and a bypassed check is worse than an absent one), and ADR-042 (rate-limit state moves to Redis and fails open with a once-per-outage alert — ADR-028 had recorded the per-process counter as a property, and re-read it is a defect: with two instances the documented 10/min silently becomes 20/min, so the mechanism's correctness rested on an infrastructure fact recorded in no document). ADR-031 gains a fourth finding that generalises the other three: a claim about someone else’s code lives until its first execution, not until its first review — two confident, wrong statements in one week (Stripe’s v1 calls, and controller-level throttle budgets), both caught by running something rather than re-reading anything. ADR-043 closes a confirmed data leak and names its shape: the guard asks whether the caller holds a permission on ANY Membership, a list builds its scope from EVERY Membership, and nothing asks whether those are the same one — a zero-permission Waiter exported another restaurant’s transaction rows until `permittedScope` landed. Its most durable finding is not the leak: five existing tests had encoded it as the specification, three of them built on fixtures describing users the seed cannot produce. ADR-044 gives `roleId` a source at last (`GET /roles` — a required input that nothing returned for ten sprints) and closes what building it exposed: `Administrator` holds every Permission and any restaurant Owner could grant it, because every write path validated only that the id existed. Fixed as a column rather than a filtered dropdown — a filtered interface over an endpoint that accepts anything is the failure mode where the screen looks correct and the API is not. The doors were counted by grepping for the field, which found four where reasoning had found two. ADR-045 answers, as a measured fact rather than a plan, what production did with an error nobody caught: inside HTTP it was logged completely and read by nobody; outside HTTP there was no handler at all, so a rejected promise in a money job killed the process, Railway restarted it, and the trace died with it — *"a service crashing and restarting in a loop looks, from outside, exactly like a service that is working."* Closed with our own AlertService before any vendor, because the policy (what is one incident, how often may it speak, which fields may leave) is the part that matters and the part that is ours. Its dedup state stays in memory *because* ADR-042 moved rate limiting out of it — a limiter's correctness is the count, a deduplicator's is not — recorded explicitly so the precedent is not applied by resemblance. Carries an addendum to ADR-044 explaining why `roleId` was the outlier: it was the one dangerous input the API accepted but never emitted, so nothing ever defined its legitimate range and four write paths independently settled for "does the id exist". The predictive form — look for inputs accepted but never returned. ADR-046 makes the seed reconcile RolePermission instead of only adding to it — a Permission removed from seed.ts used to survive forever on any existing database, which is ADR-044's platform_only failure with the direction reversed. Falsified on a populated database rather than through db:reset, which proves nothing here because a rebuilt-from-empty database cannot distinguish "only adds" from "reconciles". Production measured by permission NAME rather than count and found exactly in sync (0 revocations, 0 grants) — and deliberately not written to, because whether the seed should run at all is the decision still open. Its consequence is stated rather than buried: running the seed is now strictly more dangerous, which makes automating it both more necessary and more dangerous at once. ADR-054 carries an Amendment (2026-09-02): closing a venue still changes nothing at Stripe, but the decision to process late webhooks rested on the connection outliving the closure, and disconnection — a Dashboard-only act by the account owner — stops those events entirely. ADR-054 carries a second Amendment (ADR-059): the platform does not disconnect connected accounts, so the post-disconnection reconciliation gap is theoretical — excluded by decision, not made impossible. ADR-023 carries an amendment: superseded for refunds by ADR-062 (bill-scoped split, tip never reversed, over-bill refused), still in force for chargebacks; original text kept. |
| [docs/adr/](adr/) | — | **ADR-053 and ADR-057 onward, one decision per file.** ADR-074 measures the one contrast failure in the system that costs money directly: on the guest terminal the pay button's own text is excellent (14.19) and the button has no edge (1.28 against white, where WCAG asks 3.0), so a stranger with ten seconds may not find it. Three options measured — a dark border (18.11 and 14.19, the only one that satisfies the rule AS a boundary), a dark fill with yellow text (18.11, no new token), or a dark terminal (removes the problem rather than treating it). None chosen; the third means overturning reasoning written down twice and re-affirmed, on a venue tablet the reader cannot adjust. ADR-073 records a class the project had only half written down: the existing rule anticipates a conditional guard whose condition is WRONG, and this is the other half — a check that does not fail but does not run, whose silence is indistinguishable from green. Found by a breakage that stayed invisible for two merged pull requests because the E2E workflow's paths filter never fired; established as fact that browser-e2e is not a required check either, so it blocks nothing whether it runs or not. Four options recorded, none chosen, and the structural point that the distinction between did-not-run and passed cannot be made by the thing that did not run. Amended once the class had appeared three times in a week: the required check loses its base-branch filter, so a stacked pull request can no longer look ready without having been verified — and the obvious next step was measured rather than proposed. A throwaway protected branch established that requiring browser-e2e while its workflow is filtered by paths blocks a documentation-only pull request PERMANENTLY, with every check on the page green, a rollup reading SUCCESS, and no exit but an administrator override; a paths filter that does not match produces no workflow run at all, whereas a job skipped inside a workflow that did trigger still reports, which is what makes moving the condition into the job the cheap fix. The branch-protection change is written down as the LAST step of whichever option is chosen, not the first. ADR-072 replaces the design system's palette on a Founder decision — the Portal is dark, the amber is abolished, and there is no light variant — but the rule that matters is not the palette: the contrast floor is now measured from the DEEPEST surface text can land on rather than from the ground, because measuring against the ground certifies a value that fails on the very card it is most likely to be used in. Four rules survived the change untouched, and they are the ones about money and hierarchy rather than taste; the sharpest is the absence of a warning colour, whose stated reason partly WAS the amber, and which stands anyway. ADR-071 makes the live email check wait for the first real venue rather than create an Organization, a Restaurant and a Stripe account to test itself: a tool that could erase an Organization by hard DELETE outside the domain rules would be more dangerous than the one inert row it removes, and creating them and accepting them is the accumulation of test entities in production that #110 had to clean out. The cost is accepted explicitly — the email path is not verified live, and the successful deploy proves only that the key is present and well-formed, since boot now requires it. ADR-070 sends the invitation and takes the token out of the API response — a token in two places means the email path is never the one anybody exercises, so the tests now read it where the recipient does. It records two things it does not fix: the consent gap (invitation acceptance writes no AgreementAcceptance, unlike registration) does not change in nature but becomes the default path for every waiter and cannot be backfilled, and the 20/min invitation limit now protects domain reputation rather than a table we own — the number is the Founder's to set. ADR-069 makes Resend the email provider and email the Outbox's SECOND consumer — the trigger IMPLEMENTATION_PLAN.md recorded in advance as the one that does not announce itself. The claim step is still missing and is not fixed here; a double dispatch is made harmless for email alone by a UNIQUE constraint and Resend's 24-hour idempotency key, and that boundary is stated rather than buried. Copying the neighbouring Stripe key regex would have refused every valid Resend key and blocked the first deploy, because a real key carries an underscore in its body — caught by reading Resend's documented format, and proven by running the naive rule. ADR-068 is an estimate, not a decision: scheduled report delivery rests on a fact established by looking rather than assuming — this system has no email capability at all, and the invitation does not send one either, it hands a token back over the API for a human to relay. The scheduling half is nearly free (three services already run on @nestjs/schedule); the delivery half is an external processor of personal data, a DPA, DNS authentication and a silent-failure mode, and the same missing capability already blocks invitations, which is the more urgent reason to choose a provider. ADR-067 builds accounting's SECOND list — the by-shift exports ADR-065 §3 decided and #139 did not deliver — as separate routes rather than a flag, with every row naming which day it means: the business date the venue calls it, and the local instants with offset it actually spanned, because rendering those in UTC hid the midnight crossing the file exists to show (found by a test, not by review). A final `unassigned` row names money carrying no `shiftId`, so a by-shift export over historical rows cannot lose money silently. Staff earnings for a period is named neutrally and carries no salary, withholding or tax column — in Model B the restaurant never receives the tip and cannot pay it, and VMI has not answered on whether a tax figure is shown or withheld. ADR-062 was accepted and superseded in part on the same day: the withdrawal window is cancelled on a fact from four years of practice — a refund returns the bill and the tip stays with the waiter, so all six options answered a question that does not exist — and the product rule is that tips are not reversed on refund, recorded as a divergence from ADR-023 to be resolved on the money axis; the live findings (a reversal never fails, the recipient goes negative, the platform pays in the same second) and the six option texts are kept because they now describe the chargeback, which stays open in THREAT_MODEL. ADR-061 decides waiter onboarding in Stripe for Model B — one person one account attached to the User, onboarding starting at invitation without blocking the shift, and no tips collected for an unverified recipient, with the two rejected alternatives (a colleague's account, the restaurant's account) recorded as ways of not knowing where a euro went; the Lithuanian individual's requirements were established against the live API, not the documentation. ADR-060 removes INDEX.md's own `version:` — the single line behind five consecutive merge conflicts with no content in any of them — and replaces it with a CI check that the register agrees, in both directions, with the documents it registers; full generation from frontmatter was rejected at four hours because the row descriptions are hand-written and stay so. ADR-059 records, with Stripe's three written statements of 2026-09-02 quoted in full, that the platform does not disconnect connected accounts — not on closure, not seasonally, not on departure — because the reset surface will grow on Stripe's own forecast and disconnection is a total loss of the venue's history; closing a venue (ADR-054) touches nothing at Stripe. Terms §4/§11 now diverge from this and are recorded as diverging, not corrected here. ADR-053 answers who owns a tip at the moment of payment — a question no earlier ADR asked, which fell out as a consequence of the charge being single: Model A (one Direct charge, the waiter&#39;s share as a Ledger obligation) is built and runs the pilot, Model B (the tip separating at payment into the waiter&#39;s own account) is the target and is blocked on a written answer from VMI or the Bank of Lithuania. Its load-bearing observation is that the `tip_payable` line is identical under both, so everything up to settlement is model-independent and proceeds now. ADR-058 makes `plan` the committed default permission mode, so AI_WORKFLOW steps 1–4 are a gate rather than an intention, with ask/deny rules derived from what this repository actually runs and a PR template that asks what was RUN versus what was only written. ADR-001–056 remain in `ARCHITECTURE_DECISIONS.md` above; the split (ADR-057) exists because two branches adding an ADR to one file always conflict — a property of that file having a single insertion point, established in the #105–#109 closure, not of merge order. The existing fifty-six were deliberately not migrated: a mechanical split of 1,570 lines can silently lose one, and nothing checks that file&#39;s integrity. An invariant refuses the same ADR number in both places. Each file carries its own version, so a single ADR is individually versionable for the first time. ADR-063 keeps Stripe account status out of the Dashboard response: every figure there is computed from our own Ledger, a capability status is a cached observation of Stripe, and folding them together would make the most-viewed screen fail on Stripe's availability — the banner takes a second call, and the reasoning is recorded so the one-call "optimisation" is answered rather than re-derived. ADR-062 gains the basis for keeping REFUND_EXCEEDS_BILL a refusal: a refund of the whole amount including the tip is never done, and an event that does not occur does not deserve a mechanism; trigger is the first real alert. ADR-064 makes the Shift an entity: a payment at 01:30 on a shift nobody has closed belongs to that shift and to today's calendar date at once, so every LedgerLine carries both labels and neither derives the other; the button beats the configured time by construction rather than by comparison, the setting is per restaurant, and four questions are shown rather than decided — a shift that closes by neither route, reopening, a shift longer than a day, and what moving the Dashboard and Analytics off the calendar would cost. ADR-064's four open questions are answered: a hung shift is alerted and left open, there is no reopening, a shift longer than a day is named on screen, and 05:00 stands as the value rather than a placeholder — with the original options kept, not deleted, because their triggers name the moment someone must read them again. ADR-065 splits reporting by reader rather than by preference: operational screens read shifts, accounting gets a shift list and a calendar list from the same rows, and bank reconciliation stays calendar because Stripe counts by its own days. ADR-066 adds the Accountant role — exactly reports.view and data.export, deliberately not a reduced Manager, attached through an ordinary Membership — and establishes by execution rather than by reading that adding a role revokes nothing and does not trip the ADR-046/048 reconciliation gate, with that check kept as a test rather than a one-off observation. |
| [SYSTEM_ARCHITECTURE.md](SYSTEM_ARCHITECTURE.md) | 2.3.0 | The system's components, modules, and how they fit together — the technical shape the ADRs decided into. Documents Railway production hosting, the shared `AlertService`, and `PaymentReconciliationService` (ADR-031/032). |
| [DATABASE.md](DATABASE.md) | 2.18.0 | Every entity, field, and relationship in the data model — the source `schema.prisma` is translated from. `User.display_name` and the revised `Payment.waiter_membership_id` (ADR-033). Soft Deletes carries one recorded exception — Sprint 13 test Payments hard-deleted from production — and, more importantly, why `User`/`AuditLog` were deliberately NOT deleted (`audit_log.user_id` is ON DELETE SET NULL). ADR-061: a waiter's Stripe recipient account lives on User, never on a Membership — one person, one account, KYC once — with status fields derived from the live account and a CHECK refusing the empty-string id; measured against the live API for an LT individual, twelve requirements at creation and no identity document. Refund rules rewritten for ADR-062: a refund reverses revenue and fee over the bill and never the tip; a refund larger than the bill is refused before any write; chargebacks keep ADR-023. Adds Shift (ADR-064): a restaurant's working day, not the calendar day, with one open shift per venue enforced by a partial unique index, two unequal ways to close, and LedgerLine carrying both a calendar instant and a shift. Adds the seeded Accountant role (ADR-066): reports.view and data.export only, grantable by a venue, not a reduced Manager. |
| [API_Contract.md](API_Contract.md) (title: API_SPECIFICATION) | 2.21.0 | Every REST endpoint — request/response shape, auth requirements, rate limits — the contract the frontend and backend both build against. New `GET /restaurants/{id}/staff`, `waiter_membership_id` on Create Payment, `display_name` on Register/Accept Invitation, `PASSWORD_BREACHED` error code (ADR-032/033), and `GET /memberships/{id}/wallet` (ADR-039). **Sprint 14 casing sweep:** every request body field, response field and query parameter here is camelCase, matching the code — the document had been snake_case throughout, having borrowed the database’s convention (DATABASE.md) for the wire. Caught by the first real HTTP request the e2e harness ever made. A Field Naming section now states the boundary explicitly, and the five names that are correctly snake_case (Stripe’s own, and our database columns) are listed so they are not “fixed” later. The sweep also found `GET /restaurants` documenting a `?organization_id=` filter that was never built, and Sorting/Search recorded as conventions nothing implements. Dashboard gains todayTransactions and averageBill (null, never 0, when there were no sales); the Transaction list gains tip, net of refunds and identical to the detail screen's netTip; GET /profile gains displayName. Dashboard is shift-scoped (ADR-065): today* fields renamed shift*, a shift object carrying closedAfterMidnight and afterMidnightRevenue, and a chart of the last 7 shifts rather than 7 calendar days. Analytics JSON counts shifts; its CSV exports stay calendar, from the same rows. |
| [UX_MAP.md](UX_MAP.md) | 2.5.0 | Every screen, navigation path, and interaction in the product, and what changed on each screen as a result of specific ADRs. Sprint 13 frontend review adds the previously-missing entry flow (Register → Log In → Create Your Restaurant → Connect Payments, plus the "created but not yet live" condition, ADR-009), settles the Dashboard section definitions that had no agreed meaning, adds the terminal staff picker to the customer Payment Flow (ADR-033), corrects Analytics to ADR-027, and removes screens for concepts that do not exist (Customer, Receipt, Subscription/Billing, Notifications/Integrations/Security, Theme). Employee Details’ Wallet section is reachable at last (ADR-039). |
| [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) | 2.0.0 | **Part 1** — the design decisions that survive a change of palette: the Dashboard's emotional contract ("today is a good day" — an answer to *how are we doing?*, not *is anything wrong?*), the Hierarchy Law that ranks elements by meaning-per-second rather than by data volume, how a figure carries a caveat it cannot be honest without (ADR-026's `todayRevenueNote`), and — promoted here to a first-order product requirement — the zero state, because a screen designed to convey "today is a good day" conveys the opposite far more forcefully when the figures are zero. Names three distinct zero states and the mirror risk of making them all reassuring — reassurance comes from explanation, never suppression, or a real outage becomes indistinguishable from a quiet morning. Two rules bind Part 2 before it is written: accent colour never carries importance (size and space do, and the test is whether the hierarchy survives the accent being removed), and the customer terminal shares the Portals' foundation but chooses its own surface treatment — because on that one surface the user is not ours, gets ten seconds, and has no reason to try again. The accent must also be a token from the first line of CSS, never a literal: the deferred terminal-branding candidate (`MASTERPLAN.md`) needs exactly one swappable accent, which is free to build in now and a sweep of every component to retrofit — semantic colour is never customisable, because a failed payment must look failed in every restaurant. **Product Identity On Screen** (v1.1.0) — the product is PlainTabs, the wordmark is typographic and never the accent, it is the rank-1 element on every screen reachable before authenticating, and it is absent from the terminal payment step because a guest needs to trust the restaurant rather than us. Found by the Founder when the first login screen shipped without a name: the gap was this document’s, not the screen’s. Records the distinction that stops someone “filling in” a correctly sparse screen — sparseness is right, a missing name is a missing element. **Part 2** — the visual layer, written to one standard: *measured, or it does not exist*. Three surfaces (light Portal by default, dark as preference, pure-white terminal for sunlight), a warm-neutral ramp, one accent token with five verified values (default `#9A5D14`, and the record notes plainly that the default is the weakest of its own set at 5.19:1), a reserved semantic pair, **no warning colour at all** — deliberately, since the two places one would be used are the two Part 1 forbids alarming — IBM Plex Sans with mandatory tabular figures, and the Hierarchy Law made numeric so a review can fail on arithmetic rather than opinion. Carries one visible retraction — the dark-Portal justification, withdrawn after it turned out to cite a room no document describes and a screen this product does not have — and two transcription corrections found only by turning the tables into code: the 0.62 hierarchy constant forbade its own example (30 ÷ 48 = 0.625), and `--rule` pointed at a value that was not on the ramp at all. The tokens are now enforced rather than described: `apps/frontend/src/styles/tokens.contrast.spec.ts` re-derives every ratio from `tokens.css` itself and fails on drift. |
| [DOMAIN_GLOSSARY.md](DOMAIN_GLOSSARY.md) | 1.0.0 | One-page dictionary of project-specific terms and confusable pairs, compiled from DATABASE.md/ARCHITECTURE_DECISIONS.md/MASTERPLAN.md — never a new decision of its own. |
| [EVENT_CATALOG.md](EVENT_CATALOG.md) | 1.4.0 | The concrete catalog of `OutboxEvent` rows the codebase actually produces and their payload shapes, read directly from the code, not designed ahead of it. Notes the real ADR-031 alerting webhook alongside the pre-existing log-only alert. |
| [PERSONAL_DATA_MAP.md](PERSONAL_DATA_MAP.md) | 1.3.0 | Corrected 2026-09-05 after drifting a second time in the same direction: OutboxEvent, EmailDelivery and Shift were absent entirely and six User columns with them, while DATABASE.md named all twenty-four models throughout — the schema was documented and only the personal-data consequences were not. The OutboxEvent entry is the sharpest thing in the document: for an invitation its payload carries the recipient address AND the raw token, a working credential for seven days; the body is redacted on successful delivery and NOT on failure, and since nothing anywhere deletes an OutboxEvent, the retention of an unsent invitation body is indefinite. The recipient address is kept after redaction by design, and erasure reaches neither that table nor EmailDelivery, so a completed erasure leaves the address in both. Closes with four options for stopping a third drift, none chosen, and the observation that decides between them: a model-level invariant would have passed straight through the User miss, because that miss was six columns on a model the map already named. A factual inventory of every field identifying a person, what it links to, and what breaks if it is removed — research only, no decisions. Written because two Stripe obligations found in Sprint 14 (our own terms referencing the Connected Account Agreement, and a privacy policy linking Stripe's) both depend on knowing the answer. Its central structural finding: the boundary between "a person" and "a subject of a financial record" already runs at Membership.id, because every financial reference points at a Membership and never at a User — so the apparent conflict between erasure and the Ledger is not real. The conflicts that ARE real: AuditLog.user_id is ON DELETE SET NULL, so deleting a User blanks its own audit trail; displayName is load-bearing for three presentation joins; and AuditLog.metadata is unbounded Json, the one field where personal data can accumulate with nobody deciding it should. Its most consequential line — *a GDPR erasure request has no code path at all* — was closed by ADR-052: `redact:user` empties the person and keeps the financial record, is unreachable over HTTP by an enforced invariant, and reports every run as partial while the ipAddress/userAgent decision stays open. Now also carries §6, retention: ten years for transaction-connected data, the general contract rule otherwise — and the correction that the waiter GPM classification never blocked it. |
| [LEGAL_CLAIMS_VERIFICATION.md](LEGAL_CLAIMS_VERIFICATION.md) | 1.0.0 | Every statement the Terms of Service and Privacy Policy make about how this system behaves, checked against the code with a file and line per verdict. Two claims must not ship as written: that we hold no phone or address (Restaurant.phone and .address are required columns, and for a sole trader they are the individual&#39;s own), and the retention periods (no mechanism applies any of them — deleteMany appears nowhere in the backend). Also records what is not verifiable in code at all: backups, TLS, sub-processors, Stripe&#39;s own retention. |
| [UX_API_RECONCILIATION.md](UX_API_RECONCILIATION.md) | 1.0.0 | Every Restaurant Portal and Waiter Portal screen in `UX_MAP.md` checked against the routes that actually exist, with a file and line per verdict, in four categories — correct, stale, missing, and returned-but-unmapped. Two Dashboard sections have no data source at all (Today's Transactions, Average Bill), the Dashboard carries none of the Stripe status its own banner needs, and the Waiter Portal has roughly one and a half of its five screens reachable — a Waiter holds zero Permissions and `GET /transactions` requires `reports.view`. Separately and ranked higher: four refund/chargeback routes are documented in `API_Contract.md` as current read-only endpoints and do not exist in code. Records that its own route scanner was wrong first, and that the contract diff produced three false findings before being checked against context. |
| [BLOCK_CLOSURE_105_109.md](BLOCK_CLOSURE_105_109.md) | 1.0.0 | The five-point closure pass for PR #105–#109: live verification against production (and what could only be covered by tests, named rather than worked around), the permission-coverage audit that found 36 of 57 routes unguarded and classified each, full document reconciliation, ADR contradictions, and the list of what remains unproven. Its own finding about itself: the closure trigger had lived in memory and failed. |
| [BLOCK_CLOSURE_110_116.md](BLOCK_CLOSURE_110_116.md) | 1.0.0 | The five-point closure for PR #110–#116, and the first one the trigger called rather than a person remembering. No new unguarded routes; the three reachability-only routes from #109 are all still open, and the first of them is an action, not a read. The pre-pilot gate verified refusing in production with a real request. Two documents changed without a version bump — both in #112, the last PR to merge before #113 landed the invariant that would have caught them. |
| [BLOCK_CLOSURE_117_156.md](BLOCK_CLOSURE_117_156.md) | 1.0.0 | Part 1 of the closure for PR #117–#156 — the documentation axis, and the first closure the trigger did not call: the line said "after #121" and thirty-six pull requests went by, so the counter written down to replace memory was itself checked by memory. Eighteen triggers established by fact rather than by re-reading their conditions, against production counts read inside the container. Three had fired unremarked, and the sharpest is the one the plan predicted in its own words: email became the Outbox's second consumer, which was the trigger for the missing claim step, and the sprint was about the projection exactly as foretold. Three entries the plan calls open are closed, and one of those stale lines answers the question with teeth — it says the seed never runs automatically, while `railway.backend.json` has run it on every deploy since ADR-048 (correctly, and without `--allow-revocations`, which is the detail that makes it safe). The pre-pilot terms gate covers one of the two routes that create a User, and ADR-070 just made the other one the normal path for waiters. `PERSONAL_DATA_MAP.md` has drifted a second time and in the same direction: `OutboxEvent` — which now carries an invitation's recipient address and a live token — `EmailDelivery` and `Shift` are absent from it while `DATABASE.md` names all twenty-four models, so the schema was documented and only the personal-data consequences were not. Register and links are clean, by a CI mechanism rather than by attention; the instrument that checked them produced a false finding on its first falsification run and was fixed before it was believed. Every finding carries a basket — fix now, plan with a trigger, accept and record — and one is ranked first and separately, because it is the only one read in a panic rather than at leisure. The secrets trigger fired by a route its own wording did not describe — not a file on this machine but access to production secrets, through a local process holding the production environment and through execution inside the container — so its condition is rewritten here and the three options recorded under it, all about files, no longer fit. |
| [BLOCK_CLOSURE_117_156_AUDIT.md](BLOCK_CLOSURE_117_156_AUDIT.md) | 1.1.0 | Part 2 of the closure for PR #117–#156 — the audit axis: what does not work although it looks like it does. A fifth cause of conditional silence, established by execution rather than argued: `pnpm --recursive run` exits 0 when no package has the script, so a gate step can run, print a success line per package, and have executed nothing. It is distinct from the four known causes because those decide whether a check runs and this one lives inside a step that did run — "nothing to do" reported identically to "did it, and it passed". Four of six workspace packages have a `test` script that is an `echo`; the exposure today is nil, because three of them hold one three-line placeholder each and nothing imports them, and the risk is the transition nothing marks. Nine assertions of the form `expect(res.status).not.toBe(200)` sit in the two access-control end-to-end specs — recorded as seventeen until the fix measured it, an audit miscounting its own finding, including the six that stand over cross-Organization reads: they pass on 403, and equally on 404, 500 and on the route being deleted, which is the standard CLAUDE.md sets for a test that proves nothing. The contract declares 67 routes against 62 built; the four refund and chargeback reads #126 found are still unbuilt and still carry no marker saying so, while `GET /settings` is unbuilt and says so in the same line — the difference is one clause. Unguarded routes are unchanged at 36 while five were added, all guarded. The route instrument was wrong three times before its output was believed — a class-level permission decorator, paired route lines, and routes quoted in prose — each correction arriving because the real document had one convention more than the fixture. |
| [BLOCK_CLOSURE_117_156_PROCESS.md](BLOCK_CLOSURE_117_156_PROCESS.md) | 1.1.0 | Part 3 of the closure for PR #117–#156 — the process axis, no code. The Founder's hypothesis about quality after the move to auto mode is recorded as UNATTRIBUTABLE and closed rather than left open: three variables moved in one window with no interval separating them, so ADR-058's principle that a cause is isolable exactly once applies to the session as well as to a commit, and the proxies that could still be measured would produce numbers without attribution, which invites a conclusion they cannot support. Stacking measured from the GitHub timeline rather than recalled: four of thirty-eight were stacked at some point, one auto-closed when its base branch was deleted and had to be reopened, three were retargeted silently — so a stacked pull request that survives looks afterwards exactly like one that never was. Fifteen force-pushes over thirteen pull requests. The register is the real shared insertion point: 33 of 38 merges touched INDEX.md, 19 of them wrote to the single ADR row, and 6 changed nothing else in the file — ADR-060 removed that file's version line, which was an instance, and left the single insertion point, which is the property, so conflicts continued. That observation becomes the fifth recurring class and the one that explains the others: the remedy fits the instance, not the property, with four examples in this block, and the tell is available at the time — when a remedy is narrower than the sentence stating the problem, that gap is the next instance. The second class gains a harder sub-shape: an instrument that errs by finding NOTHING, because a filter on an identifier that does not exist returns a clean zero, which a dirty fixture cannot catch and only a positive control from the real corpus can. Carries the classification the fix pull request must settle first: the nine weak status assertions are not uniformly `.toBe(403)`, because THREAT_MODEL.md refuses with 404 where confirming a resource exists is itself the disclosure. |
| [BLOCK_CLOSURE_117_156_CLEANUP.md](BLOCK_CLOSURE_117_156_CLEANUP.md) | 1.1.0 | Part 4 of the closure for PR #117–#156, and the only part allowed to change anything. Twenty-three local branches and two remote ones deleted, each against its own pull request's state rather than against `git rev-list` — which reported nineteen of twenty as holding unmerged work, because squash-merge leaves no original commit on main, so the obvious test would have preserved every dead branch forever and quietly. One branch had no pull request at all, and its disposition was settled from the code instead: all twenty-two files present in main, fourteen byte-identical, and the commit that introduced its headline file names PR #67 — absent a record, the file's own history is the record. The development database reset after carrying 267 Outbox events, 193 unpublished against a fifty-row batch, from three suite runs in a day. Kept and named: the Founder's editor config, build output that regenerates, and every dependency — no unreferenced one was found, reported as a weak claim because the check is a substring search. Records the suite-interference variant into the plan's own entry: everything that entry describes is accumulation ACROSS runs, and the failure measured was interference INSIDE one, on a database reset moments earlier, which the existing counter cannot observe because it measures at setup when the debris does not exist yet — so a cleanup hook between files would leave it untouched and only per-file rollback closes both. Shows without deciding whether the three empty packages should exist at all: each is three lines of `export {}` imported by nobody, each cites the "nothing just in case" rule in the comment explaining why it exists just in case, and deleting them closes part 2's fifth silence cause for free — against which `packages/types` has a named consumer and `packages/**` sits inside a path filter currently under review. |
| [THREAT_MODEL.md](THREAT_MODEL.md) | 1.19.0 | Security threats considered for the platform — organized into Closed Threats, Accepted Risk, and Open, Not Answered, each citing the ADR that resolved or owns it. Sprint 13 (ADR-032/033) closes five entries (payment reconciliation, dependency scanning, breached-password check, waiter tip-recipiency, idempotency race) and reclassifies PROCESSOR_CLEARING alongside the Wallet-availability entry, both blocked on `Withdrawal`. Closed Threat 14 now also cites real external scanning caught in production (Leakix, 2026-08-20). Entry 22 records the production-backup gap, the PITR restore rehearsal that closed it (including what it did NOT prove), and the layered PITR + Daily/Weekly/Monthly snapshot coverage; volume-snapshot restore sits in "Open, Not Answered" — a different mechanism, never rehearsed, and not safely rehearsable against production. Two entries added 2026-09-02 after Stripe confirmed in writing that a disconnected account's events never reach the platform endpoint and that disconnection is Dashboard-only: a chargeback after disconnection never reaches our books and reconciliation reports a difference it cannot resolve, and the platform is blind to the disconnection itself — the party who acts is the account owner, not us. Closed Threat 27 (2026-09-02): the onboarding-link route needed three measures, not one — an existing Permission checked at the venue (`restaurant.create`, 404 not 403), a 10/hour throttle, and a status gate that is deferred because no sandbox account has completed onboarding. Two of its earlier inferences are now measured: `account_update` is refused for our accounts, and a link expires in exactly five minutes. Both disconnection entries reformulated under ADR-059: the risk arrives only if the no-disconnect decision is broken, by us or by an owner acting alone; kept open, not closed, because a decision is not a lock. Open entry added 2026-09-02: a chargeback reverses the whole charge, tip included, and the bank decides — not treated with a withdrawal window; trigger is the first real chargeback or a number from the pilot. |
| [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) | 2.29.0 | The sprint-by-sprint build plan — tasks and Definition of Done for each sprint, Sprint 0 through the current one, plus a Deferred/Not-Yet-Scheduled backlog for Prisma 5→7, MFA, the off-platform backup, the staging environment (the last two both triggered by the first real pilot restaurant), and refresh-token revocation surviving a Redis outage — the last being ADR-019's own named trigger firing, deferred until after the frontend because the fix re-opens ADR-019's original latency trade-off rather than patching around it; the vitest 2→3 item is now closed (ADR-037). Adds error-rate monitoring as ADR-045's step 2, carrying the Founder's four standing conditions on Sentry — chief among them that pino and Sentry must share one redaction list, since two lists diverge and the second silently stops matching. Adds a backlog entry on working files holding secrets: several were written to a scratch directory during Sprint 14, cleaned up with no live key surviving, and nothing prevents the next one — the deny and ignore rules are shaped around one filename convention and a copy escapes them by not using it. |
| [SEQUENCE_DIAGRAMS.md](SEQUENCE_DIAGRAMS.md) | 1.0.1 | Prose explanation of what each sequence diagram resolves and the ADRs that came out of walking through the timing (ADR-014, ADR-015, ADR-016). |
| [SEQUENCE_ONBOARDING.md](SEQUENCE_ONBOARDING.md) | — (no frontmatter; mermaid source) | Mermaid sequence diagram for restaurant creation through Stripe Connect onboarding to first-payment readiness. |
| [SEQUENCE_PAYMENT_TIP.md](SEQUENCE_PAYMENT_TIP.md) | — (no frontmatter; mermaid source) | Mermaid sequence diagram for a customer payment plus tip, from client-side confirmation through webhook-driven Ledger/Wallet updates. |
| [SEQUENCE_REFUND_CHARGEBACK.md](SEQUENCE_REFUND_CHARGEBACK.md) | — (no frontmatter; mermaid source) | Mermaid sequence diagram for staff-initiated refunds and Stripe-initiated chargebacks, including the provisional-loss/reversal pattern. |
| [SPRINT_0_SCHEMA_AUDIT.md](SPRINT_0_SCHEMA_AUDIT.md) | 1.2.0 | Closed audit of the initial `schema.prisma` translation from DATABASE.md v2.0.0 — open questions and their resolutions, Founder-reverified. |
| [ARCHITECTURE_REVIEW_REPORT.md](ARCHITECTURE_REVIEW_REPORT.md) | 1.0.0 | Final cross-document review searching for contradictions, dead architecture, and gaps across the Sprint 0 documentation package. |
| [CONCEPT_VISION_RU.md](CONCEPT_VISION_RU.md) | 1.0 (no frontmatter) | Original Russian-language internal concept document — vision, product, business model, monetization, roadmap; the earliest-stage pitch this project grew from. |
